"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FeatureGates = exports.GateID = void 0;
const statsig_node_1 = __importDefault(require("statsig-node"));
const crypto_1 = require("@atproto/crypto");
const logger_1 = require("./logger");
var GateID;
(function (GateID) {
    /**
     * Left here ensure this is interpreted as a string enum and therefore
     * appease TS
     */
    GateID["_"] = "";
})(GateID || (exports.GateID = GateID = {}));
/**
 * @see https://docs.statsig.com/server/nodejsServerSDK
 */
class FeatureGates {
    constructor(config) {
        Object.defineProperty(this, "config", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: config
        });
        Object.defineProperty(this, "ready", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: false
        });
        Object.defineProperty(this, "statsig", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: statsig_node_1.default
        });
        Object.defineProperty(this, "ids", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: GateID
        });
    }
    async start() {
        try {
            if (this.config.apiKey) {
                /**
                 * Special handling in test mode, see {@link ServerConfig}
                 *
                 * {@link https://docs.statsig.com/server/nodejsServerSDK#local-overrides}
                 */
                await this.statsig.initialize(this.config.apiKey, {
                    localMode: this.config.env === 'test',
                    environment: {
                        tier: this.config.env || 'development',
                    },
                });
                this.ready = true;
            }
        }
        catch (err) {
            logger_1.featureGatesLogger.error({ err }, 'Failed to initialize StatSig');
            this.ready = false;
        }
    }
    destroy() {
        if (this.ready) {
            this.ready = false;
            this.statsig.shutdown();
        }
    }
    async user({ did }) {
        const userID = await (0, crypto_1.sha256Hex)(did);
        return {
            userID,
        };
    }
    check(user, gate) {
        if (!this.ready)
            return false;
        return this.statsig.checkGateSync(user, gate);
    }
}
exports.FeatureGates = FeatureGates;
//# sourceMappingURL=feature-gates.js.map