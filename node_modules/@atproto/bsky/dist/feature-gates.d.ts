import { StatsigUser } from 'statsig-node';
export type Config = {
    apiKey?: string;
    env?: 'development' | 'staging' | 'production' | string;
};
export declare enum GateID {
    /**
     * Left here ensure this is interpreted as a string enum and therefore
     * appease TS
     */
    _ = ""
}
/**
 * @see https://docs.statsig.com/server/nodejsServerSDK
 */
export declare class FeatureGates {
    private config;
    ready: boolean;
    private statsig;
    ids: typeof GateID;
    constructor(config: Config);
    start(): Promise<void>;
    destroy(): void;
    user({ did }: {
        did: string;
    }): Promise<StatsigUser>;
    check(user: StatsigUser, gate: GateID): boolean;
}
//# sourceMappingURL=feature-gates.d.ts.map