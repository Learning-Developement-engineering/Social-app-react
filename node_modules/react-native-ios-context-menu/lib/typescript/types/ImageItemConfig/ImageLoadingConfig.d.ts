import type { ImageItemConfig } from "./ImageItemConfig";
export declare type ImageLoadingConfig = {
    shouldCache?: boolean;
    shouldLazyLoad?: boolean;
};
export declare type ImageRemoteUrlConfig = {
    url: string;
    fallbackImage?: ImageItemConfig;
};
export declare type ImageRemoteURLFallbackBehavior = 'afterFinalAttempt' | 'whileNotLoaded' | 'onLoadError';
export declare type ImageRemoteURLLoadingConfig = ImageLoadingConfig & {
    maxRetryAttempts?: number;
    shouldImmediatelyRetryLoading?: boolean;
    fallbackBehavior?: ImageRemoteURLFallbackBehavior;
};
