import { requireNativeComponent, View, Platform } from 'react-native';
const viewName = "RNIContextMenuButton";
/**
 * Do not use `RNIContextMenuButton` if platform is not iOS.
 */

export const RNIContextMenuButton = Platform.select({
  ios: () => requireNativeComponent(viewName),
  default: () => View
})();
//# sourceMappingURL=RNIContextMenuButton.js.map