import { requireNativeComponent, Platform, View } from 'react-native';
const viewName = "RNIContextMenuView";
/**
 * Do not use `RNIContextMenuView` if platform is not iOS.
 */

export const RNIContextMenuView = Platform.select({
  ios: () => requireNativeComponent(viewName),
  default: () => View
})();
//# sourceMappingURL=RNIContextMenuView.js.map