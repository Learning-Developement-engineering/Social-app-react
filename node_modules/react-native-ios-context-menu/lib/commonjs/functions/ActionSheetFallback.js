"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ActionSheetFallback = void 0;

var Helpers = _interopRequireWildcard(require("./Helpers"));

function _getRequireWildcardCache(nodeInterop) { if (typeof WeakMap !== "function") return null; var cacheBabelInterop = new WeakMap(); var cacheNodeInterop = new WeakMap(); return (_getRequireWildcardCache = function (nodeInterop) { return nodeInterop ? cacheNodeInterop : cacheBabelInterop; })(nodeInterop); }

function _interopRequireWildcard(obj, nodeInterop) { if (!nodeInterop && obj && obj.__esModule) { return obj; } if (obj === null || typeof obj !== "object" && typeof obj !== "function") { return { default: obj }; } var cache = _getRequireWildcardCache(nodeInterop); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

// TODO: Refactor/cleanup...
// TODO: Fix types + remove `@ts-ignore`
class ActionSheetFallbackHelpers {
  /** is the object a `MenuAction` item */
  static isObjectMenuAction(object) {
    return (// required properties --------
      'actionKey' in object || 'actionTitle' in object || // optional properties --------------
      'menuState' in object || 'menuAttributes' in object
    );
  }

  /** is the object a `MenuObject` item */
  static isObjectMenuConfig(object) {
    return (// required properties --------
      'menuTitle' in object || // optional properties -----
      'menuOptions' in object || 'menuItems' in object
    );
  }

  /** based on the `indexPath`, return the corresponding `MenuConfig` item */
  static getItemFromMenuConfig(menuConfig, indexPath) {
    if (indexPath == null) return null;
    if (menuConfig == null) return null;
    let currentObject = menuConfig;

    for (const index of indexPath) {
      var _currentObject, _currentObject2, _currentObject2$menuI;

      if (currentObject == null) {
        return null;
      } else if (((_currentObject = currentObject) === null || _currentObject === void 0 ? void 0 : _currentObject.type) === 'deferred') {
        return null;
      } else if (ActionSheetFallbackHelpers.isObjectMenuAction(currentObject)) {
        return currentObject;
      } else if (!ActionSheetFallbackHelpers.isObjectMenuConfig(currentObject)) {
        return null;
      }

      ;
      currentObject = (_currentObject2 = currentObject) === null || _currentObject2 === void 0 ? void 0 : (_currentObject2$menuI = _currentObject2.menuItems) === null || _currentObject2$menuI === void 0 ? void 0 : _currentObject2$menuI[index];
    }

    ;
    return currentObject;
  }

  /** handle inline submenu's by hoisting submenu action to the parent menu */
  static flattenMenuConfig(menuConfig) {
    if (menuConfig == null) return null;
    let indexPath = [0];
    let currentParent = menuConfig;

    const incrementCurrentIndexPath = () => {
      const indexPathCount = indexPath.length; // increment last index path

      indexPath[indexPathCount - 1]++;
    };

    const getCurrentIndexPath = () => indexPath[indexPath.length - 1];

    const getCurrentParentItemsCount = () => {
      var _currentParent$menuIt, _currentParent, _currentParent$menuIt2;

      return (_currentParent$menuIt = (_currentParent = currentParent) === null || _currentParent === void 0 ? void 0 : (_currentParent$menuIt2 = _currentParent.menuItems) === null || _currentParent$menuIt2 === void 0 ? void 0 : _currentParent$menuIt2.length) !== null && _currentParent$menuIt !== void 0 ? _currentParent$menuIt : 0;
    };

    const didReachEnd = () => getCurrentIndexPath() >= getCurrentParentItemsCount() - 1; // traverse menu config until last item reached


    while (true) {
      let currentItem = ActionSheetFallbackHelpers.getItemFromMenuConfig(menuConfig, indexPath);
      const isCurrentItemEmpty = currentItem == null;

      if (!isCurrentItemEmpty && ActionSheetFallbackHelpers.isObjectMenuConfig(currentItem)) {
        var _menuOptions$includes, _menuOptions, _menuItems$length, _menuItems;

        // current item is menu config, check if inline submenu
        const isInlineSubmenu = (_menuOptions$includes = (_menuOptions = currentItem.menuOptions) === null || _menuOptions === void 0 ? void 0 : _menuOptions.includes('displayInline')) !== null && _menuOptions$includes !== void 0 ? _menuOptions$includes : false;
        const inlineSubmenuItemsCount = (_menuItems$length = (_menuItems = currentItem.menuItems) === null || _menuItems === void 0 ? void 0 : _menuItems.length) !== null && _menuItems$length !== void 0 ? _menuItems$length : 0;

        if (isInlineSubmenu && inlineSubmenuItemsCount > 0) {
          var _currentParent2, _inlineMenu$0$menuIte, _inlineMenu$, _currentParent3;

          const inlineMenu = (_currentParent2 = currentParent) === null || _currentParent2 === void 0 ? void 0 : _currentParent2.menuItems.splice(getCurrentIndexPath(), 1);
          const inlineMenuItems = // @ts-ignore
          (_inlineMenu$0$menuIte = inlineMenu === null || inlineMenu === void 0 ? void 0 : (_inlineMenu$ = inlineMenu[0]) === null || _inlineMenu$ === void 0 ? void 0 : _inlineMenu$.menuItems) !== null && _inlineMenu$0$menuIte !== void 0 ? _inlineMenu$0$menuIte : [];
          (_currentParent3 = currentParent) === null || _currentParent3 === void 0 ? void 0 : _currentParent3.menuItems.splice(getCurrentIndexPath(), 0, ...inlineMenuItems);
        } else {
          // update currentParent
          // @ts-ignore
          currentParent = ActionSheetFallbackHelpers.getItemFromMenuConfig(menuConfig, indexPath); // and traverse down...

          indexPath.push(0);
        }

        ;
      } else if (didReachEnd()) {
        // reached last item, go to next item
        indexPath.pop();
        incrementCurrentIndexPath();
        const indexPathCount = indexPath.length;
        const nextIndexPath = indexPath.slice(0, indexPathCount - 1); // @ts-ignore

        currentParent = indexPathCount <= 0 ? menuConfig : ActionSheetFallbackHelpers.getItemFromMenuConfig(menuConfig, nextIndexPath); // last menu config item reached, stop...

        if (didReachEnd() || indexPath.length < 1) break;
      } else if (ActionSheetFallbackHelpers.isObjectMenuAction(currentItem)) {
        // current item is menu action, skip...
        incrementCurrentIndexPath();
      } else {
        // current item unknown item, skip...
        incrementCurrentIndexPath();
      }

      ;
    }

    ;
    return menuConfig;
  }

  /** returns the index of the first destructive action */
  static getdestructiveButtonIndex(menuItems) {
    if (menuItems == null) return null;

    for (let index = 0; index < menuItems.length; index++) {
      var _menuItem$menuAttribu, _menuItem$menuAttribu2, _menuItem$menuOptions, _menuItem$menuOptions2;

      const menuItem = menuItems[index]; // @ts-ignore

      const isDestructiveAction = (_menuItem$menuAttribu = menuItem === null || menuItem === void 0 ? void 0 : (_menuItem$menuAttribu2 = menuItem.menuAttributes) === null || _menuItem$menuAttribu2 === void 0 ? void 0 : _menuItem$menuAttribu2.includes('destructive')) !== null && _menuItem$menuAttribu !== void 0 ? _menuItem$menuAttribu : false; // @ts-ignore

      const isDestructiveMenu = (_menuItem$menuOptions = menuItem === null || menuItem === void 0 ? void 0 : (_menuItem$menuOptions2 = menuItem.menuOptions) === null || _menuItem$menuOptions2 === void 0 ? void 0 : _menuItem$menuOptions2.includes('destructive')) !== null && _menuItem$menuOptions !== void 0 ? _menuItem$menuOptions : false;

      if (isDestructiveAction || isDestructiveMenu) {
        return index;
      }

      ;
    }

    ;
    return null;
  }

  /** remove disabled/hidden menu actions */
  static filterMenuItems() {
    let menuItems = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
    if (menuItems == null) return [];
    return menuItems.filter(menuItem => {
      var _menuItem$menuAttribu3, _menuItem$menuAttribu4, _menuItem$menuAttribu5, _menuItem$menuAttribu6;

      // @ts-ignore
      const isDisabledAction = (_menuItem$menuAttribu3 = menuItem === null || menuItem === void 0 ? void 0 : (_menuItem$menuAttribu4 = menuItem.menuAttributes) === null || _menuItem$menuAttribu4 === void 0 ? void 0 : _menuItem$menuAttribu4.includes('disabled')) !== null && _menuItem$menuAttribu3 !== void 0 ? _menuItem$menuAttribu3 : false; // @ts-ignore

      const isHiddenAction = (_menuItem$menuAttribu5 = menuItem === null || menuItem === void 0 ? void 0 : (_menuItem$menuAttribu6 = menuItem.menuAttributes) === null || _menuItem$menuAttribu6 === void 0 ? void 0 : _menuItem$menuAttribu6.includes('hidden')) !== null && _menuItem$menuAttribu5 !== void 0 ? _menuItem$menuAttribu5 : false;
      const isDeferredElement = (menuItem === null || menuItem === void 0 ? void 0 : menuItem.type) === 'deferred';
      return !isDisabledAction && !isHiddenAction && !isDeferredElement;
    });
  }

}

;

class ActionSheetFallback {
  /** based on a `MenuConfig` object, show an `ActionSheetIOS` menu  */
  static async show(menuConfig) {
    let indexPath = [];
    let actionSheetOptions = [];
    let cancelButtonIndex = 0;
    let destructiveButtonIndex = null;
    const menuConfigCopy = JSON.parse(JSON.stringify(menuConfig)); // handle 'inlineMenu' submenu's

    ActionSheetFallbackHelpers.flattenMenuConfig(menuConfigCopy);

    while (true) {
      var _menuItem$menuItems;

      const menuItem = ActionSheetFallbackHelpers.getItemFromMenuConfig(menuConfigCopy, indexPath); // selected item is an action, exit...

      if (ActionSheetFallbackHelpers.isObjectMenuAction(menuItem)) {
        return menuItem;
      }

      ; // selected item is menu, get submenu and remove hidden/disable actions
      // @ts-ignore

      const menuItems = ActionSheetFallbackHelpers.filterMenuItems((_menuItem$menuItems = menuItem === null || menuItem === void 0 ? void 0 : menuItem.menuItems) !== null && _menuItem$menuItems !== void 0 ? _menuItem$menuItems : []); // add menu items to action sheet

      actionSheetOptions = menuItems.map(item => {
        var _ref, _item$actionTitle;

        return (// @ts-ignore
          (_ref = (_item$actionTitle = item === null || item === void 0 ? void 0 : item.actionTitle) !== null && _item$actionTitle !== void 0 ? _item$actionTitle : // @ts-ignore
          item === null || item === void 0 ? void 0 : item.menuTitle) !== null && _ref !== void 0 ? _ref : null
        );
      }); // set destructive index and offset by 1 bc of cancel button

      destructiveButtonIndex = ActionSheetFallbackHelpers.getdestructiveButtonIndex(menuItems);
      if (destructiveButtonIndex != null) destructiveButtonIndex++; // wait for selected item

      const selectedIndex = await Helpers.asyncActionSheet({ // action sheet config
        ...(cancelButtonIndex != null && {
          cancelButtonIndex
        }),
        ...(destructiveButtonIndex != null && {
          destructiveButtonIndex
        }),
        options: ['cancel', ...actionSheetOptions]
      }); // guard: cancel button pressed, exit...

      if (selectedIndex === cancelButtonIndex) return null; // push selected index to indexPath and loop
      // @ts-ignore

      indexPath.push(selectedIndex - 1);
    }

    ;
  }

}

exports.ActionSheetFallback = ActionSheetFallback;
;
//# sourceMappingURL=ActionSheetFallback.js.map