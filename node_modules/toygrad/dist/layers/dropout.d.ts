import AbstractHidden from './abstract_hidden';
import type Abstract from './abstract';
import type Tensor from '../tensor';
import type { DropoutOptions } from '../types';
declare class Dropout extends AbstractHidden<DropoutOptions> {
    probability: number;
    dropped: Float32Array;
    constructor(options: DropoutOptions, prev?: Abstract);
    forward(input: Tensor, isTraining: boolean): Tensor;
    backward(): void;
}
export default Dropout;
