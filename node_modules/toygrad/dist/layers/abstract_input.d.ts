import Abstract from './abstract';
import type { AbstractInputOptions } from '../types';
declare class AbstractInput<T extends AbstractInputOptions = AbstractInputOptions> extends Abstract<T> {
}
export default AbstractInput;
