import AbstractInput from './abstract_input';
import type Abstract from './abstract';
import type Tensor from '../tensor';
import type { InputOptions } from '../types';
declare class Input extends AbstractInput<InputOptions> {
    constructor(options: InputOptions, prev?: Abstract);
    forward(input: Tensor, isTraining: boolean): Tensor;
}
export default Input;
