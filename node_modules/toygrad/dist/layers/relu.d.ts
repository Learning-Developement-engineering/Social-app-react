import AbstractActivationElementwise from './abstract_activation_elementwise';
import type { ReluOptions } from '../types';
declare class Relu extends AbstractActivationElementwise<ReluOptions> {
    activationForward(x: number): number;
    activationBackward(x: number, dx: number): number;
}
export default Relu;
