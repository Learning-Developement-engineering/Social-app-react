import AbstractHidden from './abstract_hidden';
import Tensor from '../tensor';
import type Abstract from './abstract';
import type { PoolOptions } from '../types';
declare class Pool extends AbstractHidden<PoolOptions> {
    sx: number;
    sy: number;
    stride: number;
    pad: number;
    switchX: Float32Array;
    switchY: Float32Array;
    constructor(options: PoolOptions, prev?: Abstract);
    forward(input: Tensor, isTraining: boolean): Tensor;
    backward(): void;
}
export default Pool;
