import { StatsigOptions } from './StatsigOptions';
import StatsigContext from './utils/StatsigContext';
export declare const ExceptionEndpoint = "https://statsigapi.net/v1/sdk_exception";
export default class ErrorBoundary {
    private sdkKey;
    private optionsLoggingCopy;
    private statsigMetadata;
    private seen;
    constructor(sdkKey: string, optionsLoggingCopy: StatsigOptions, sessionID: string);
    swallow<T>(task: (ctx: StatsigContext) => T, ctx: StatsigContext): void;
    capture<T>(task: (ctx: StatsigContext) => T, recover: (e: unknown) => T, ctx: StatsigContext): T;
    setup(sdkKey: string): void;
    private onCaught;
    logError(error: unknown, ctx: StatsigContext): void;
    private getDescription;
    private beginMarker;
    private endMarker;
}
